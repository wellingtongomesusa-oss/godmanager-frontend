{
  "name": "godmanager-frontend",
  "private": true,
  "scripts": {
    "dev": "npm run dev --prefix ./GodManager",
    "dev:godmanager": "npm run dev --prefix ./GodManager",
    "dev:godmanager2": "npm run dev --prefix \"./GodManager 2\"",
    "build": "npm run build --prefix ./GodManager",
    "start": "npm run start --prefix ./GodManager",
    "lint": "npm run lint --prefix ./GodManager"
  }
}
