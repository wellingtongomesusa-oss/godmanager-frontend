{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "cd GodManager && npx prisma generate && npm run build",
    "watchPatterns": ["GodManager/**"]
  },
  "deploy": {
    "startCommand": "cd GodManager && npx prisma migrate deploy && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
