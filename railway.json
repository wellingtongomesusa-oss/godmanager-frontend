{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS",
    "buildCommand": "echo GMBUILD_SHA_CHECK=[$RAILWAY_GIT_COMMIT_SHA] SOURCE=[$SOURCE_VERSION] && npx prisma generate && npm run build",
    "watchPatterns": ["GodManager/**"]
  },
  "deploy": {
    "startCommand": "npx prisma migrate deploy && npm start",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 3
  }
}
